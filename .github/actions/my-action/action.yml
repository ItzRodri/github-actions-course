name: "My action"
description: "My action description"

inputs:
  message:
    description: "The message to print"
    required: true
    default: "Hello world Rodrigo"

runs:
  using: "composite"
  steps:
    - name: Hola Mundo
      run: echo "Hello ${{ inputs.message }}"
      shell: bash
